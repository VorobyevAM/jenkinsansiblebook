pipeline {
    parameters {
        string(name: 'HOST')
    }

    agent any

    options{
        ansiColor('xterm')
        timestamps()
    }
    stages {
        stage('Install node_exporter') {
            steps {
                  sh 'ansible-playbook create_user/playbook1.yml -i my_hosts.yml -u root -p root -e "host=${HOST}"'
            }
        }
    }
}