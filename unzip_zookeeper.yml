---
- name: "Test playbook run"
  hosts: all
  become: true
  remote_user: kafka
  tasks:
    - name: unzip zookeeper
      ansible.builtin.unarchive:
        src: /home/kafka/apache-zookeeper-*-bin.tar.gz
        dest: /opt/zookeeper
